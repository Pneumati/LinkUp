import 'package:flutter/material.dart';

class Viewstuappt extends StatelessWidget {
  const Viewstuappt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("View Appointement"),
      ),
    );
  }
}
