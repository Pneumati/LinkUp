import 'package:flutter/material.dart';

class Bookaptsu extends StatelessWidget {
  const Bookaptsu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Book Appointement "),
      ),
    );
  }
}
